{{- if .Values.serviceAccount.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "ticket-service.name" . }}-role-binding
  namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "ticket-service.name" . }}-role
subjects:
  - kind: ServiceAccount
    name: {{ include "ticket-service.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}