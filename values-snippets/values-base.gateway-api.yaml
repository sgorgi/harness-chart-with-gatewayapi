# Merge this block into the base or common values file used by each release.
gatewayAPI:
  enabled: false
  environment: ""

  gateway:
    name: apps-gateway
    namespace: envoy-gateway-apps

  listenerSet:
    name: harness
    namespace: envoy-gateway-apps

  referenceGrant:
    name: harness-gateway-tls

  httpRedirect:
    enabled: false
    statusCode: 301
