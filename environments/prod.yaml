# Environment-specific Gateway API settings belong here.
# Hostnames, TLS secrets, paths, services, and ports are discovered from the
# Harness manifests rendered with values-base.yaml and values-prod.yaml.
gatewayTransition:
  enabled: true
  mode: shadow
