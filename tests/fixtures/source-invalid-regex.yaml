global:
  ingress:
    enabled: true
  gatewayAPI:
    enabled: true

optionalIngress:
  enabled: false

looker:
  enabled: false

regexIngress:
  enabled: true
  path: /proxy(?=unsupported-lookahead)

unknownAnnotationIngress:
  enabled: false
