# Generated ListenerSets and HTTPRoutes, plus the stable supporting resources,
# are rendered only when this overlay is enabled.
harnessGatewayOverlay:
  enabled: false

  # Opt in only after confirming the Envoy data-plane labels and the existing
  # default-deny policies in the target cluster.
  ciliumNetworkPolicy:
    enabled: false
